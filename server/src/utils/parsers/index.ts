import { parseNumber } from './parseNumber';
import { parseRequired } from './parseRequired';
import { parseString } from './parseString';

export const envHelpers = { parseString, parseNumber, parseRequired };
